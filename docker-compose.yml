version: "3.8"
services:
  nodelogs:
    image: nodebackend:latest
    container_name: node-backend
    restart: always
    build: .
    ports:
      - "5003:5001"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
